# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.128.0
uvicorn[standard]==0.40.0          # includes uvloop, httptools, websockets, watchfiles
starlette==0.50.0
python-multipart==0.0.21           # file upload support

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.45
aiosqlite==0.20.0                  # async SQLite driver
alembic==1.18.1                    # database migrations
greenlet==3.3.0                    # required by SQLAlchemy async

# ── Data validation ───────────────────────────────────────────────────────────
pydantic==2.12.5
pydantic-settings==2.12.0
pydantic_core==2.41.5
annotated-types==0.7.0
typing_extensions==4.15.0
typing-inspection==0.4.2

# ── LLM / AI ──────────────────────────────────────────────────────────────────
openai==2.15.0                     # Deepseek API (OpenAI-compatible client)
tiktoken==0.12.0                   # token counting
tenacity==9.1.2                    # retry logic for LLM calls

# ── HTTP client ───────────────────────────────────────────────────────────────
httpx==0.28.1
httpcore==1.0.9
anyio==4.12.1
sniffio==1.3.1

# ── Auth / crypto (JWT, password hashing) ────────────────────────────────────
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
cryptography==46.0.3
bcrypt==5.0.0

# ── Configuration ─────────────────────────────────────────────────────────────
python-dotenv==1.2.1

# ── Utilities ─────────────────────────────────────────────────────────────────
requests==2.32.5                   # used in git_service for HTTP operations
regex==2026.1.15                   # advanced regex (used by tiktoken)
distro==1.9.0                      # OS detection (openai dependency)
jiter==0.12.0                      # fast JSON iterator (openai dependency)
tqdm==4.67.1                       # progress bars
packaging==25.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov==7.0.0
coverage==7.13.1

# ── Code quality ──────────────────────────────────────────────────────────────
black==26.1.0
isort==7.0.0

# ── Documentation (MkDocs) ────────────────────────────────────────────────────
mkdocs==1.6.1
mkdocs-material==9.7.1
mkdocs-material-extensions==1.3.1
mkdocs-get-deps==0.2.0
pymdown-extensions==10.21          # admonitions, superfences, tabbed, etc.
Markdown==3.10
PyYAML==6.0.3
Jinja2==3.1.6
MarkupSafe==3.0.3
babel==2.18.0                      # i18n for Material theme
paginate==0.5.7
ghp-import==2.1.0                  # GitHub Pages deployment helper
watchdog==6.0.0                    # mkdocs serve file watching
mergedeep==1.3.4
pyyaml_env_tag==1.1
pathspec==1.0.3
backrefs==6.2
colorama==0.4.6
click==8.3.1
